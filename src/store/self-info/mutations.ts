import { MutationTree } from 'vuex'
import { SelfInfoStateInterface } from './state'

const mutation: MutationTree<SelfInfoStateInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
}

export default mutation
